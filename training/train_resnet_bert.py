import json
from pathlib import Path
import pandas as pd
import torch

BASE_DIR = Path(__file__).resolve().parent.parent

def main():
    print("Model 1 training script initialized.")
    print("Baseline Model 1 results in results/model_1_early_fusion are strictly protected.")

if __name__ == "__main__":
    main()
